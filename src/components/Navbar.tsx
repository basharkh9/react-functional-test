import { AppBar, Toolbar } from "@mui/material";

const Navbar = () => {
  return (
    <AppBar position="sticky">
      <Toolbar>React Functional Test</Toolbar>
    </AppBar>
  );
};

export default Navbar;
